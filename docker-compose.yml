version: '3.8'

services:
  iris-lab:
    image: site_iris_lab:latest
    build: .
    container_name: site_iris_lab
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - iris-lab-network

networks:
  iris-lab-network:
    driver: bridge
